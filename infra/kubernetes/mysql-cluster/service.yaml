apiVersion: v1
kind: Service
metadata:
  name: mysql-headless-service
  namespace: default
spec:
  selector:
    app: mysql
  clusterIP: None
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
